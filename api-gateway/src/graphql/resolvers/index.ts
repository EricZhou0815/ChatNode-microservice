import * as Mutation from "../Mutation"
import * as Query from "../Query"
import UserSession from "./UserSessionResolvers"

const resolvers = { Mutation, Query, UserSession }

export default resolvers